// In Vite, /profile and /profile/:userId both rendered <Profile />.
// NOTE: the current Profile ignores the userId param and always shows the
// logged-in user. Wire router.query.userId into Profile when you build it out.
export { default } from './index';
