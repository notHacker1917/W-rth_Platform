import type { NextPage } from 'next';

const Page: NextPage = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>API Page</h1>
      <p>Backend API server is running. Use the frontend at http://localhost:5174</p>
    </div>
  );
};

export default Page;
